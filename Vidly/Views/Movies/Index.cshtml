@model Vidly.ViewModel.CustomerViewModel
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Movies</h2>

@foreach (var movie in Model.Movies)
{
    <tr><td>@Html.ActionLink(@movie.MovieName , "Details", "Customers", new { id = @movie.MovieId }, null)</td></tr>
}